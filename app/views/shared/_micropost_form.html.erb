<%= form_for @micropost do |f| %>
  <%= render "shared/error_messages", object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: t("post.compose") %>
  </div>
  <%= f.submit t("post.post"), class: "btn btn-primary" %>
<% end %>
