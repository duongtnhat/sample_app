<li>
  <%= gravatar_for user, size: Settings.constant.avatar_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("user.delete"), user, method: :delete,
        data: {confirm: t("user.confirm_delete")} %>
  <% end %>
</li>
