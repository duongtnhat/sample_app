<% provide :title, t("user.all_user") %>
<h1><%= t "user.all_user" %></h1>
<%= paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>
<%= paginate @users %>
